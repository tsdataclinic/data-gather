/**
 * This file stores any constants that are used to configure the app.
 * Any sensitive configuration variables, or environment-specific
 * configurations, should live in .env files.
 */

/**
 * A dictionary of language codes to their display names in their respective
 * language.
 */
export const LANGUAGES: Record<string, string> = {
  en: 'English',
  es: 'Español',
};
